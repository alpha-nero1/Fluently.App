import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    languageItem: {
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center',
        padding: 8,
        height: 60,
        flex: 1
    }
});